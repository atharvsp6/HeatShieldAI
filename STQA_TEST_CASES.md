# STQA Test Cases: HeatShield AI Web Application

| TC ID | Feature | Test Scenario | Test Data | Steps | Expected Result | Test Type |
|---|---|---|---|---|---|---|
| **TC001** | User Authentication | Successful login using valid administrator credentials | `username: "admin"`, `password: "admin123"` | 1. Navigate to HeatShield AI login page.<br>2. Clear and enter "admin" into `#login-username`.<br>3. Clear and enter "admin123" into `#login-password`.<br>4. Click "Sign In" button. | Successfully authenticated; redirected to Command Center; sidebar displays user "System Administrator" with role "ADMIN". | Positive / Functional |
| **TC002** | User Authentication | Quick-access demo role selection and login | Demo role button `#demo-role-meteorologist` | 1. Navigate to login page.<br>2. Click `#demo-role-meteorologist` button.<br>3. Verify input fields auto-populate.<br>4. Click "Sign In" button. | Form auto-populates `meteorologist` / `met123`; authentication succeeds; user is logged in as "Dr. Priya Sharma" with role "METEOROLOGIST". | Positive / Functional |
| **TC003** | User Authentication | Login failure with invalid password | `username: "admin"`, `password: "wrongpass123"` | 1. Navigate to login page.<br>2. Enter "admin" into `#login-username`.<br>3. Enter "wrongpass123" into `#login-password`.<br>4. Click "Sign In" button. | Authentication rejected with HTTP 401; user remains on login page; red error banner displays "Invalid username or password". | Negative / Error Handling |
| **TC004** | User Authentication | Login failure with non-existent username | `username: "unknown_user_99"`, `password: "anypass"` | 1. Navigate to login page.<br>2. Enter "unknown_user_99" into `#login-username`.<br>3. Enter "anypass" into `#login-password`.<br>4. Click "Sign In" button. | Authentication rejected with HTTP 401; red error banner displays "Invalid username or password"; no JWT issued. | Negative / Error Handling |
| **TC005** | User Authentication | Submit button disabled when credentials are missing | `username: ""`, `password: ""` | 1. Navigate to login page.<br>2. Leave username and password empty. | "Sign In" button has `disabled` attribute; cursor is not pointer; clicking button does not dispatch network request. | Boundary / Input Validation |
| **TC006** | Advisory Review | Approve a pending heatwave advisory | Logged in as METEOROLOGIST | 1. Navigate to "Advisories" page from sidebar.<br>2. Switch to "Citizens" audience tab.<br>3. Locate a card with "pending" status pill.<br>4. Click "Approve" button on the card. | Advisory card status transitions from "pending" to "approved" (green pill); action buttons update to outline "Regenerate". | Positive / State Transition |
| **TC007** | Advisory Review | Reject a pending heatwave advisory | Logged in as METEOROLOGIST | 1. Navigate to "Advisories" page.<br>2. Select a card with "pending" status.<br>3. Click "Reject" button on the card. | Advisory card status transitions from "pending" to "rejected" (red pill); rejection is stored in database with `rejected_at` timestamp. | Positive / State Transition |
| **TC008** | Advisory Review | Audience stakeholder tab navigation | Audience keys: `CITIZENS`, `AUTHORITIES`, `FARMERS`, `HEALTHCARE` | 1. Navigate to Advisories page.<br>2. Click each audience tab sequentially: Authorities, Farmers, Healthcare, Citizens. | Active tab highlights; cards filter strictly to selected audience; count badges match displayed items. | Positive / Functional |
| **TC009** | Advisory Review | State persistence across page navigation | Advisory approved in TC006 | 1. Approve or reject an advisory.<br>2. Navigate to "Overview" page via sidebar.<br>3. Navigate back to "Advisories" page. | Previously approved/rejected advisory retains its modified status; does not revert to pending. | Positive / State Persistence |
| **TC010** | Weather Station Search | Filter weather stations table by matching city name | Query: `"Delhi"` | 1. Log in and navigate to "Weather Stations".<br>2. Enter "Delhi" into station search input field. | Table filters dynamically; only stations in the Delhi region are displayed (e.g., `AWS-DEL-001`); row count updates accordingly. | Positive / Functional |
| **TC011** | Weather Station Search | Filter weather stations by station code prefix | Query: `"AWS-JAI"` | 1. Navigate to "Weather Stations".<br>2. Enter "AWS-JAI" into search input field. | Only Jaipur automated weather stations matching prefix `AWS-JAI` are displayed in the table. | Boundary / Search Precision |
| **TC012** | Weather Station Search | Search query yielding zero matching stations | Query: `"NonExistentCityXYZ"` | 1. Navigate to "Weather Stations".<br>2. Enter "NonExistentCityXYZ" into search field. | Table displays 0 station rows; empty state component displays "No stations match your filters". | Negative / Boundary |
| **TC013** | Weather Station Search | Interactive table column sorting | Column header: "Temperature" | 1. Navigate to "Weather Stations".<br>2. Click "Temperature" column header once.<br>3. Click "Temperature" column header a second time. | First click sorts rows in descending order (highest temp first); second click reverses sort to ascending order. | Positive / Table Interaction |
| **TC014** | Automation Settings | Toggle automation setting and verify persistence | Setting: "Auto-refresh dashboard" (`auto_refresh`) | 1. Navigate to "Settings" page.<br>2. Locate "Auto-refresh dashboard" option.<br>3. Click the switch toggle button. | Toggle switch flips visual position; `aria-checked` toggles between `"true"` and `"false"`; change is persisted to PostgreSQL via `PUT /api/settings`. | Positive / State Persistence |
